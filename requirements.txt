# Core dependencies for Orbital Dynamics Labs
vastpy>=0.3.17
pyyaml>=6.0
vastdb>=0.1.0

# VAST DB SDK dependencies (from https://github.com/vast-data/vastdb_sdk)
ibis-framework==9.0.0
pyarrow>=10.0.0
pandas>=1.5.0

# Development and testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional dependencies for enhanced functionality
requests>=2.28.0
python-dotenv>=1.0.0

# S3 support for file uploads
boto3>=1.26.0
botocore>=1.29.0

# Rich terminal formatting for monitoring dashboard
rich>=13.0.0
