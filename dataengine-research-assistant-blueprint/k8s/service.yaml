apiVersion: v1
kind: Service
metadata:
  name: research-assistant
  labels:
    app: research-assistant
spec:
  type: ClusterIP
  selector:
    app: research-assistant
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP

